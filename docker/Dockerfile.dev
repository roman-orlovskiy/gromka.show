FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install --no-audit --no-fund

COPY . .

EXPOSE 8080

CMD ["sh", "-c", "npx prisma generate && npx nuxt dev --host 0.0.0.0 --port 8080"]

